<%= flash[:notice] %>
<%= link_to 'Add candidate', new_candidate_path%>

<table>
    <tr>
        <td>Name</td>
        <td>Party</td>
        <td>Age</td>
        <td>Politics</td>
        <td>Action</td>
    </tr>
    <% @candidates.each do |candidate| %>
    <tr>
        <td><%= link_to candidate.name, candidate_path(candidate.id) %></td>
        <td><%= candidate.party %></td>
        <td><%= candidate.age %></td>
        <td><%= candidate.politice %></td>
        <td>Action</td>
    </tr>
    <% end %>
</table>